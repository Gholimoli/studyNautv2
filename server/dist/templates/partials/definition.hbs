{{!-- 
  Partial: definition.hbs
  Context: LessonBlock (this)
  Renders a definition block. Assumes term is in 'title' and definition in 'content'.
--}}
<div class="p-4 my-4 border-l-4 border-blue-500 bg-blue-50">
    <p class="font-semibold text-blue-800">Definition{{#if title}}: {{title}}{{/if}}</p>
    <div class="mt-2 text-blue-700">
        {{{content}}}
    </div>
</div> 