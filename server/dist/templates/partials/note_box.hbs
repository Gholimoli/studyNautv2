{{!-- 
  Partial: note_box.hbs
  Context: Assumes 'content' field contains the text. Optional 'title'.
  Description: Renders content as a note callout.
--}}
{{#if content}}
<div class="p-4 my-4 border-l-4 border-gray-500 bg-gray-50">
    <p class="font-semibold text-gray-800">Note{{#if title}}: {{title}}{{/if}}</p>
    <div class="mt-2 text-gray-700">
        {{{content}}}
    </div>
</div>
{{/if}} 