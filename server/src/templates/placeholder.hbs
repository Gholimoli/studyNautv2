<div class="my-6 p-4 border border-dashed border-border rounded-lg bg-muted/50 text-center">
  <p class="text-sm text-muted-foreground italic">
    Visual placeholder: {{concept}}
  </p>
  {{#if errorMessage}}
    <p class="text-xs text-destructive mt-1">({{errorMessage}})</p>
  {{/if}}
</div> 